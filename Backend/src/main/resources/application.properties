# =========================================================
# = CONFIGURACIÓN GENERAL
# =========================================================
spring.application.name=portfolio
server.port=${PORT:8080}

# =========================================================
# = BASE DE DATOS (PostgreSQL en Render)
# =========================================================
spring.datasource.url=jdbc:postgresql://dpg-d44fktkhg0os73cer42g-a.oregon-postgres.render.com/portfolio_rqj1
spring.datasource.username=nerea
spring.datasource.password=zs11x35eJNyZVcMYlPraXOuK602BfTwA
spring.datasource.driver-class-name=org.postgresql.Driver

# Opcional: si Render te da error con conexión SSL, usa esto 👇
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Permite conexión SSL obligatoria en Render
spring.datasource.hikari.data-source-properties.sslmode=require

# =========================================================
# = JPA / HIBERNATE
# =========================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# =========================================================
# = SEGURIDAD / JWT
# =========================================================
jwt.secret=MiSuperClaveJWTDeProduccionNereaFernandez2025Segura
jwt.expiration=36000000  # 10 horas

# =========================================================
# = LOGGING
# =========================================================
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# =========================================================
# = ARCHIVOS / MULTIPART
# =========================================================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
